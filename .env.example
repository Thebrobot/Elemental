# Copy to .env — never commit real secrets
# Google Analytics 4 (optional, public)
PUBLIC_GA4_ID=

# Override contact endpoint for local testing (optional)
# PUBLIC_CONTACT_ENDPOINT=http://localhost:8888/.netlify/functions/contact
